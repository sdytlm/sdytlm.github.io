<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Tag: virtualization | 卖萌的弱渣]]></title>
  <link href="http://sdytlm.github.io/tags/virtualization/atom.xml" rel="self"/>
  <link href="http://sdytlm.github.io/"/>
  <updated>2015-04-28T09:57:37-07:00</updated>
  <id>http://sdytlm.github.io/</id>
  <author>
    <name><![CDATA[Min Li]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Build Nested KVM Virtualization]]></title>
    <link href="http://sdytlm.github.io/blog/2015/04/07/Install-Nested-Virtualization/"/>
    <updated>2015-04-07T00:00:00-07:00</updated>
    <id>http://sdytlm.github.io/blog/2015/04/07/Install-Nested-Virtualization</id>
    <content type="html"><![CDATA[<h3>Platform</h3>

<ul>
<li>Dom0: Opensuse 13.1</li>
<li>Host/Guest Hypervisor: KVM</li>
<li>Guest OS: CentOS</li>
</ul>


<!--more -->


<h3>Enable Nested in Host KVM</h3>

<ol>
<li>Check if nested is enabled in host hypervisor</li>
</ol>


<pre><code>cat /sys/module/kvm_intel/parameters/nested
</code></pre>

<p>if the result is <code>Y</code>. Nested virgualization in your host is open. Ohterwise you need to complete the followings</p>

<ol>
<li>`Modify /etc/default/grub</li>
</ol>


<blockquote><p>GRUB_CMDLINE_LINX = &ldquo;kvm-intel.nested=1 &rdquo;</p></blockquote>

<ol>
<li>Update grub</li>
</ol>


<pre><code>grub2-mkconfig -o /boot/grub2/grub.cfg
</code></pre>

<ol>
<li>Reboot and check again</li>
</ol>


<h3>Install the guest Hypervisor</h3>

<p>I use the raw disk as the VM partition
<figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>virt-install &ndash;name=centos5.6 &ndash;os-variant=RHEL5 &ndash;ram=1024 &ndash;vcpus=1 &ndash;disk path=/dev/sda4,format=raw,bus=virtio &ndash;accelerate &ndash;cdrom /Your DVD / &ndash;vnc &ndash;vncport=5910 &ndash;vnclisten=0.0.0.0 &ndash;network bridge=br0,model=virtio &ndash;noautoconsole&lt;/p&gt;
</span><span class='line'>
</span><span class='line'>&lt;pre&gt;&lt;code&gt;
</span><span class='line'>### Configure the Guest Hypervisor
</span><span class='line'>
</span><span class='line'>* Disable cache on guest disk
</span><span class='line'>In xml file, do
</span><span class='line'>&lt;/code&gt;&lt;/pre&gt;
</span><span class='line'>
</span><span class='line'>&lt;p&gt;&lt;driver name='qemu' type='raw' cache='none'/&gt;</span></code></pre></td></tr></table></div></figure></p>

<ul>
<li><p>Enable VMX in guest CPU
In xml file, do
<code>
&lt;cpu match='exact'&gt;
&lt;model&gt;core2duo&lt;/model&gt;
&lt;feature policy='require' name='vmx'/&gt;
&lt;/cpu&gt;
</code></p></li>
<li><p>Check guest CPU feature in host
In host OS</p></li>
</ul>


<pre><code>ps -ef | grep qemu-kvm
</code></pre>

<ul>
<li><p>Disable SELinux
In SELinux
<code>
SELINUX=permissive
</code></p></li>
<li><p>Configure network</p></li>
</ul>


<pre><code>service NetworkManager stop
chkconfig NetworkManager off
chkconfig network on
yum install bridge-utils
</code></pre>

<p>eth0:</p>

<pre><code>DEVICE=eth0
TYPE=Ethernet
ONBOOT=yes
NM_CONTROLLED=yes
BRIDGE=br0
</code></pre>

<p>br0</p>

<pre><code>DEVICE=br0
NM_CONTROLLED=yes
ONBOOT=yes
TYPE=Bridge
BOOTPROTO=dhcp
</code></pre>

<p>reboot your computer</p>

<ul>
<li>Compile and Install QEMU</li>
</ul>


<pre><code>./configure
make
make install
</code></pre>

<ul>
<li>Install libvirt and management tool</li>
</ul>


<pre><code>yum install libvirt-client virt-viewer guestfish libguestfs-tools virt-top libvrt python-virtinst 
</code></pre>

<ul>
<li>Launch libvirtd</li>
</ul>


<pre><code>service libvirtd start
chkconfig libvirtd on
</code></pre>

<ul>
<li>Check if libvirtd is turned on</li>
</ul>


<pre><code>virsh list
</code></pre>

<h3>Virsh Command</h3>

<ul>
<li>Launch a VM</li>
</ul>


<pre><code>virsh start domain name
</code></pre>

<ul>
<li>Undefine a VM</li>
</ul>


<pre><code>virsh destroy domain name
virsh undefine domain name
</code></pre>

<ul>
<li>Connect a VM</li>
</ul>


<pre><code>virt-viewer -c qemu:///system  domain name
</code></pre>

<ul>
<li>Delete a VM</li>
</ul>


<pre><code>virsh destroy domain name
virsh undefine domain name
</code></pre>

<ul>
<li>Add VCPU to a VM</li>
</ul>


<pre><code>virsh shutdown domain Name
virsh edit domain name
</code></pre>

<p>do this
<code>
edit &lt;vcpu placement='static'&gt;4&lt;/vcpu&gt;
virsh create /etc/libvirt/qemu/yourconfig.xml
</code></p>

<ul>
<li>Solve the CPU incompatibility problems</li>
</ul>


<p>In host, run virsh commands to check CPU compatibility.
<code>
virsh  capabilities | virsh cpu-baseline /dev/stdin
</code>
Copy past the display in your VM XML file</p>

<p>For example</p>

<pre><code>&lt;cpu match='exact'&gt;
  &lt;model&gt;Penryn&lt;/model&gt;
  &lt;vendor&gt;Intel&lt;/vendor&gt;
  &lt;feature policy='require' name='dca'/&gt;
  &lt;feature policy='require' name='xtpr'/&gt;
  &lt;feature policy='require' name='tm2'/&gt;
  &lt;feature policy='require' name='vmx'/&gt;
  &lt;feature policy='require' name='ds_cpl'/&gt;
  &lt;feature policy='require' name='monitor'/&gt;
  &lt;feature policy='require' name='pbe'/&gt;
  &lt;feature policy='require' name='tm'/&gt;
  &lt;feature policy='require' name='ht'/&gt;
  &lt;feature policy='require' name='ss'/&gt;
  &lt;feature policy='require' name='acpi'/&gt;
  &lt;feature policy='require' name='ds'/&gt;
  &lt;feature policy='require' name='vme'/&gt;
&lt;/cpu&gt;
</code></pre>
]]></content>
  </entry>
  
</feed>
